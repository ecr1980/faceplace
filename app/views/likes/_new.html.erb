<%= form_with model: @like do |f| %>
  <%= f.hidden_field :liker_id, :value => current_user.id %>
  <%= f.hidden_field :likable_type, :value => post_type %>
  <%= f.hidden_field :likable_id, :value => saying.id %>
  <%= f.submit "Like" %>
<% end %>